apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taskwave-frontend.fullname" . }}-config
  labels:
    {{- include "taskwave-frontend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
