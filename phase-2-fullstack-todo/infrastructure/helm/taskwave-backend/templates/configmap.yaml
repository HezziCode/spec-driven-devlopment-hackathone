apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "taskwave-backend.fullname" . }}-config
  labels:
    {{- include "taskwave-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
